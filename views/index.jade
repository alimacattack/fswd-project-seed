extends ./layout.jade

block body
  div(ng-controller="uvIndexController")
    if user
      | Hello #{user.username}!
    else
      | Hello world!

    p The current UV Index is
      {{currentUvIndex.UV_VALUE}}
